<%= form_with model: password do |form|%>
  <div>
    <%=form.label :url%>
    <%=form.url_field :url%>
  </div>
  <div>
  <%=form.label :username%>
    <%=form.text_field :username%>
  </div>
  <div>
    <%=form.label :password%>
    <%=form.text_field :password%>
  </div>
  <div>
    <%= form.submit%>
    <%= button_tag "Delete", form: "delete_password" if current_user_password.deletable? %>
  </div>
<%end%>

<%= form_with model: password, method: :delete, id: "delete_password", data: { confirm: "Are you sure?" } %>
